# Must match the JWT_SECRET in apps/api/.env exactly — middleware verifies
# the token's signature independently of the Express backend.
JWT_SECRET=

# Where the Express API (apps/api) is reachable from the Next.js server.
EXPRESS_API_URL=http://localhost:5000/api
